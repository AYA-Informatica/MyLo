# Base URL of the MyLo API, including the /api/v1 prefix.
# Local development against the Docker Compose stack:
VITE_API_BASE_URL=http://localhost:5001/api/v1

# Google OAuth client ID (public by design — it ships in the client bundle).
VITE_GOOGLE_CLIENT_ID=
